export type techDTO = {
    content:string,
    created_at:string,
    id:number,
    title:string
}

export type personalDTO = {
    id: number,
    title: string,
    content: string,
    created_at: string,
    personal_id: number,
    image_url: string
}

export type reelsDTO = {

}